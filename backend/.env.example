# Desenvolvimento LOCAL (fora do Docker) — Postgres exposto na porta do host
DATABASE_URL=postgresql+psycopg://edusaas:edusaas@localhost:5434/edusaas
APP_ENV=development
CORS_ORIGINS=http://localhost:3000
JWT_SECRET=5b65f57b4dac6ebbcd6705d7c50d2b33111e99f041d925713f03f12f47b11f28
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7
PUBLIC_BASE_URL=http://localhost:8000

# IA — geração de provas via Claude Code CLI
# Assinatura (sem chave de API): rode `claude setup-token` e cole o token aqui.
CLAUDE_CODE_OAUTH_TOKEN=
# Alternativa: chave de API (https://console.anthropic.com/settings/keys).
ANTHROPIC_API_KEY=
# Opcional: força um modelo específico do CLI (ex.: claude-sonnet-4-6). Vazio usa o padrão.
CLAUDE_MODEL=
